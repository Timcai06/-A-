\section{中长期油价调节模型（60--180 天）}

\subsection{情景设定}

中长期油价调节模型对应赛题任务 2。本文在短期冲击模型的综合最优参数基础上构建乐观、中性和悲观三种情景。赛题附件只提供到 2026 年 5 月初的真实价格，60--180 天区间没有可用于事后对照的真实观测值。因此本文采用“情景预测”和“平衡点分析”的表述：三情景曲线表示条件中心路径，可信度由机制一致性、官方外生数据校验、敏感性分析、蒙特卡洛概率区间和状态转移扰动共同评价。

短期冲击模型和中长期油价调节模型并非两套互不相干的模型。短期部分使用同一套物理缺口和市场价格形成递推式，回答封锁初期价格平台如何形成；中长期部分仍以该递推机制为中心，只是把未来不可观测的政策协调、绕道恢复、供给响应和二次升级风险从固定参数扩展为情景变量和状态变量。换言之，随着外推期限由 0--60 天延长到 60--180 天，不确定性的主要来源从“参数校准误差”逐步转向“事件状态不确定性”，因此需要用蒙特卡洛情景树和状态转移扰动描述条件区间。

中长期油价调节模型从短期冲击模型延伸而来，但在四个方面做了时间尺度调整：

\begin{enumerate}
  \item 供应侧：保持封锁造成的基础缺口，同时允许非海湾产油国和 OPEC 剩余产能缓慢响应。
  \item 缓冲侧：SPR、商业库存和绕道运输继续发挥作用，但其边际能力随时间下降或受运输瓶颈约束。
  \item 需求侧：长期需求价格弹性高于短期弹性，反映高油价持续后消费、生产和替代能源调整更充分。
  \item 风险侧：恐慌溢价逐步衰减，不确定性溢价拆分为前期冲击不确定性和持续封锁制度风险，使长期价格由物理恢复与制度风险共同决定。
\end{enumerate}

为避免短期弹性与长期弹性之间出现突变，本文采用平滑过渡函数刻画需求调整过程。设题面短期弹性为 \(\varepsilon_s=-0.05\)，长期情景目标弹性为 \(\varepsilon_\infty\)，冲击后的第 \(t_s=60\) 天作为长期外推起点，则

\begin{equation}
\varepsilon_t
=
\varepsilon_s
+
\left(\varepsilon_\infty-\varepsilon_s\right)
\left[1-\exp\left(-\alpha_\varepsilon\max(t-t_s,0)\right)\right],
\quad t\ge 0 .
\end{equation}

其中 \(\alpha_\varepsilon\) 为需求调整速度。该式表示：封锁初期消费与生产难以及时替代，弹性接近题面低弹性；高油价持续后，炼厂采购、交通消费、工业用能和替代运输逐渐调整，弹性绝对值向长期情景值平滑靠近。因此，表中长期弹性不是在第 60 天突然替换短期弹性，而是作为 \(\varepsilon_t\) 的渐近目标进入递推。

记中长期外推期第 $t$ 日有效供应为 $S_t^{(L)}$，有效需求为 $D_t^{(L)}$。在短期冲击模型的基础上，中长期供需缺口可写为

\begin{equation}
G_t^{(L)}
=
D_t^{(L)}
-
\left[
S_0-I+R_t+Q_t+B_t+A_t
\right],
\end{equation}

其中 $I$ 为供应中断量，$R_t$ 为实际 SPR 释放量，$Q_t$ 为绕道运输恢复量，$B_t$ 为商业库存缓冲量，$A_t$ 为其他产油国增产或替代供应增量。考虑到现实中的政策反馈机制，本文设置 SPR 的实际释放量为缺口与价格压力的自适应函数：

\begin{equation}
R_t=\min\left\{\bar R_t,\ \max\left(G_{t,\mathrm{pre}}+\rho D_0,\ \bar R_t \lambda_t \tau_t\right)\right\},
\end{equation}

其中 $\bar R_t$ 为计划释放能力，$G_{t,\mathrm{pre}}$ 为不含 SPR 时的预释放缺口，$\rho D_0$ 为安全余量，$\lambda_t$ 表示由物理缺口和价格压力共同决定的释放强度，$\tau_t$ 为长期持续后的政策收缩系数。二者定义为

\begin{equation}
\lambda_t
=
\mathrm{clip}\left[
\lambda_0
+\lambda_G\frac{\max(G_{t,\mathrm{pre}},0)}{D_0}
+\lambda_P\max\left(\frac{\hat P_t-P_0}{P_0},0\right),
\lambda_{\min},1
\right],
\end{equation}

\begin{equation}
\tau_t
=
\tau_{\min}
+
\frac{1-\tau_{\min}}
{1+\exp\left[\kappa_\tau(t-t_{\mathrm{shrink}})\right]} .
\end{equation}

其中 \(\mathrm{clip}(x,a,b)=\min\{\max(x,a),b\}\)，\(t_{\mathrm{shrink}}\) 对应表中 SPR 收缩起点。上述定义使 SPR 在缺口较大、价格压力较高时保持较强释放；当长期封锁进入政策收缩期且缺口逐步闭合时，\(\tau_t\) 平滑下降，实际释放量转向缺口驱动。因此，当缺口已基本闭合时，SPR 不会继续按上限满额释放。

中长期价格递推采用

\begin{equation}
\hat P_{t+1}
=
(1-v_L)\hat P_t
+v_L
\left[
P_0+\Pi(G_t^{(L)},\varepsilon_L)
\right]
+\Phi_t-\Omega_t+J_t ,
\end{equation}

其中 $\Pi(\cdot)$ 表示由剩余供需缺口和长期需求弹性 $\varepsilon_L$ 共同决定的供需压力；当 $G_t^{(L)}<0$ 时，过剩供给通过折价项向下拉动目标价格。$\Phi_t$ 由地缘风险溢价、前期冲击不确定性和持续封锁制度风险三部分构成，其中制度风险随未解决供应压力和时间信心恢复共同衰减。$\Omega_t$ 为政策缓冲确认和预期修复带来的折价，$J_t$ 为库存接近耗尽时的跳变风险项。该形式对应赛题要求中的增产、长期弹性变化、运输瓶颈和库存耗尽风险。

短期和中长期风险项的方向不同，并不是前后矛盾。短期窗口内，市场需要完成突发封锁的信息吸收，风险溢价表现为建立和再定价；中长期窗口内，市场已经观察到 SPR、库存、绕道和供需调整是否有效，因此风险项应随未解决缺口和信心恢复而逐步衰减。换言之，短期冲击模型回答“冲击如何形成价格平台”，中长期油价调节模型回答“平台在政策缓冲和需求调整后如何回归，尾部风险在哪里重新抬升”。

\begin{table}[!htbp]
\centering
\caption{长期机制强度参数及检验边界}
\begin{tabularx}{\linewidth}{Y S[table-format=2.3] L{2.2cm} Y}
\toprule
机制参数 & {基准值} & 扰动范围 & 含义与边界 \\
\midrule
SPR 收缩起点 & 75 & 60--105 & SPR 从应急计划释放转向缺口驱动收缩的开始时间 \\
冲击不确定性占比 & 0.45 & 0.25--0.65 & 前期冲击再定价在长期不确定性中的贡献，不作为外部官方统计量 \\
制度风险占比 & 0.90 & 0.50--1.30 & 持续封锁状态对长期风险溢价的贡献强度 \\
信心恢复后风险底座 & 0.40 & 0.20--0.60 & 缓冲机制被市场确认后仍保留的制度风险比例 \\
过剩供给回归强度 & 1.35 & 0.80--2.20 & 供给超过需求时向下修正目标价格的机制强度 \\
封锁风险衰减速度 & 0.004 & 0.002--0.008 & 封锁风险溢价随时间被市场吸收的日度速度 \\
\bottomrule
\end{tabularx}
\end{table}

\Needspace{0.50\textheight}
\subsection{中长期参数来源与校验边界}

中长期油价调节模型中的部分参数用于描述恢复速度、政策收缩和制度风险衰减。本文对中长期参数采用三条约束：方向来自赛题机制或能源市场逻辑；数值作为基准情景假设进入；关键参数进入扰动区间、敏感性排序或蒙特卡洛检验。中长期参数来源与校验方式见\cref{tab:long-parameter-audit}。

\begin{table}[!htbp]
\centering
\caption{中长期参数来源与校验}
\label{tab:long-parameter-audit}
\footnotesize
\renewcommand{\arraystretch}{1.08}
\begin{tabularx}{\linewidth}{L{2.6cm}Y Y}
\toprule
参数组 & 来源逻辑 & 校验方式 \\
\midrule
SPR 收缩与释放强度 & 来自赛题 SPR 缓冲机制，并加入长期封锁下不能无限满额释放的政策约束。 & 在 60--105 天范围内扰动，检验第 180 天价格和尾部峰值。 \\
冲击不确定性与制度风险 & 来自封锁未解除时保险、航运、二次升级和政策协调失败的持续风险。 & 与 GPR、OVX、蒙特卡洛和状态转移结果合读，不作为同步拟合因子。 \\
封锁风险衰减速度 & 表示市场对封锁信息和政策缓冲确认的消化速度。 & 设置 0.002--0.008 日衰减率区间，并在敏感性分析中单独排序。 \\
过剩供给回归强度 & 表示缓冲、增产和需求调整后，目标价格向下修正的速度。 & 通过扰动区间和情景曲线检验，防止中性路径被人为拉直。 \\
增产与绕道恢复 & 来自赛题要求的增产、运输瓶颈和绕道能力，并用 JODI 多国数据作数量级校验。 & 用于长期条件外推；JODI 按多国上报口径解释，不等同完整全球总量。 \\
\bottomrule
\end{tabularx}
\normalsize
\end{table}

\keypoint{中长期参数用于描述机制强弱和恢复速度。本文把这些参数显式列出、放入扰动区间，并用敏感性和蒙特卡洛结果报告其不确定性。}

\begin{table}[!htbp]
\centering
\caption{三情景关键参数}
\begin{tabular}{l S[table-format=4.0] S[table-format=3.0] S[table-format=3.0] S[table-format=-1.2] S[table-format=1.2] S[table-format=1.2]}
\toprule
情景 & {供应中断} & {SPR 上限} & {绕道能力} & {长期弹性} & {恐慌衰减} & {风险权重} \\
\midrule
乐观 & 1400 & 700 & 300 & -0.25 & 0.12 & 2.02 \\
中性 & 1493 & 670 & 237 & -0.25 & 0.11 & 2.46 \\
悲观 & 1800 & 200 & 150 & -0.10 & 0.04 & 3.32 \\
\bottomrule
\end{tabular}
\end{table}

三情景参数并非三组孤立猜测。乐观情景代表高 SPR 可用量、绕道恢复较充分、长期需求调整较快；悲观情景代表供应中断接近上界、SPR 释放受限、绕道能力偏低且制度风险溢价持续；中性情景则采用综合最优参数附近的中心设定。后续敏感性分析和蒙特卡洛抽样用于检验：当这些假设联合变化时，价格中心路径和尾部风险是否仍保持一致。

在补充官方外生数据后，本文将 EIA、JODI 与 OPEC 从“论文证据层”推进到“中长期参数约束层”。具体做法是由 JODI 多国同口径产量、库存、进出口、需求、炼厂产出、EIA 美国 SPR 和商业库存变化，以及 OPEC 全球供需平衡表中的需求增量、非 DoC 供给增量和 DoC 原油需求差额生成温和乘数，对绕道能力、库存缓冲、需求收缩、长期弹性、SPR 上限可信度和风险权重进行约束。得到的乘数见\cref{tab:official-factor-multipliers}。

\begin{table}[!htbp]
\centering
\caption{官方外生数据生成的中长期参数约束因子}
\label{tab:official-factor-multipliers}
\begin{tabularx}{\linewidth}{Y S[table-format=1.4] Y}
\toprule
约束因子 & {乘数} & 解释 \\
\midrule
绕道和替代贸易恢复能力 & 0.9725 & JODI 进出口同口径收缩，说明替代贸易恢复不宜过度乐观 \\
商业库存日缓冲上限 & 1.0739 & JODI 原油/成品油库存和 EIA 商业库存均提供缓冲数量级支撑 \\
长期需求收缩幅度 & 0.9560 & JODI 与 OPEC 均显示需求韧性，需求破坏不宜设得过强 \\
长期需求弹性绝对值 & 0.9750 & 高价下需求会调整，但 OPEC 正常需求增长使长期弹性略收敛 \\
SPR 释放上限可信度 & 0.9672 & EIA 美国 SPR 实际释放较温和，因此全球协调释放上限作小幅折扣 \\
贸易收缩风险权重 & 1.0833 & JODI 进出口收缩、OPEC DoC 需求差额和 OVX 风险读数共同抬升尾部风险 \\
长期不确定性强度 & 1.0749 & 贸易收缩、下半年需求差额峰值和 OVX 隐含波动率使制度风险上调 \\
\bottomrule
\end{tabularx}
\end{table}

\subsection{预测结果}

\begin{table}[!htbp]
\centering
\caption{中长期油价调节模型的 60--180 天三情景预测结果}
\begin{tabular}{l S[table-format=3.2] S[table-format=3.2] S[table-format=3.2] S[table-format=3.2] S[table-format=3.2] S[table-format=3.2] l}
\toprule
情景 & {第60天} & {第90天} & {第120天} & {第180天} & {外推期最高价} & {外推期均价} & 风险 \\
\midrule
乐观 & 109.83 & 92.83 & 91.45 & 87.05 & 101.51 & 90.88 & 低 \\
中性 & 109.83 & 104.99 & 100.30 & 93.26 & 109.72 & 100.39 & 低 \\
悲观 & 109.83 & 123.25 & 115.56 & 111.71 & 129.71 & 117.87 & 高 \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[htbp]
\centering
\includegraphics[width=\linewidth,height=0.52\textheight,keepaspectratio]{蒙特卡洛情景树高级组合图.png}
\caption{中长期油价调节模型蒙特卡洛情景树}
\label{fig:long-term-fan}
\end{figure}

\begin{figure}[htbp]
\centering
\includegraphics[width=0.94\linewidth]{短期拟合与长期预测总览.png}
\caption{短期冲击模型与中长期油价调节模型总览}
\end{figure}

\FloatBarrier

\cref{fig:long-term-fan} 给出中长期油价调节模型的主图。为刻画中心路径之外的事件状态不确定性，本文额外建立状态转移扰动层。该层不改变三情景物理中心路径，而是让未来状态在缓和、维持和升级之间随机切换：缓和状态向乐观中心靠近，维持状态围绕中性中心，升级状态向悲观中心和跳涨方向移动；扰动强度由附件历史价格波动估计后进行保守折扣。进一步地，本文用 SPR 剩余预算、库存消耗、剩余缺口、绕道瓶颈和高价压力生成物理状态压力，并用 GPR 事件月历史分位数、OVX 冲突窗口历史分位数以及滞后 OVX 与实现波动率相关性生成市场风险压力，共同温和提高早中期升级概率。这样得到的中长期图由中心路径、状态概率和尾部区间共同构成。

三状态马尔可夫链的基础转移矩阵设为

\begin{equation}
M_0=
\begin{bmatrix}
0.478 & 0.522 & 0.000\\
0.065 & 0.880 & 0.055\\
0.000 & 0.255 & 0.745
\end{bmatrix},
\quad
\text{行、列顺序均为 }(\text{缓和},\text{维持},\text{升级}).
\end{equation}

矩阵含义见\cref{tab:state-transition-matrix}。基础矩阵由 2017--2025 年附件历史价格状态序列校准得到：维持状态是最常见的中间状态，升级状态具有较强惯性，缓和状态则容易回到维持状态。外部风险读数通过综合压力 \(q\in[0,1]\) 进入时变矩阵：

\begin{equation}
q=0.6q_{\mathrm{OVX}}+0.4q_{\mathrm{GPR}},\quad
d_t=\mathrm{clip}\left(1-\frac{t-60}{150},0.25,1\right),
\quad r_t=0.75qd_t,
\end{equation}

其中 \(q_{\mathrm{OVX}}\) 由 OVX 冲突窗口历史分位数和滞后 OVX 与 7 日实现波动率相关性计算，\(q_{\mathrm{GPR}}\) 由 GPR 事件月历史分位数计算。本项目实算得到 \(q=0.831\)。同时定义物理压力 \(h_t\in[0,1]\)，由悲观中心路径中的剩余缺口、SPR 预算消耗、库存消耗、高价压力、绕道瓶颈和 SPR 耗尽压力加权得到；定义恢复证据 \(g_t\in[0,1]\)，由缺口缓解、SPR 与库存剩余、高价压力回落和耗尽压力下降共同决定。为避免直接在线性概率上加减后再裁剪，本文先将基础矩阵第 \(i\) 行 \(m_i\) 映射到 logit 空间：

\begin{equation}
\ell_{i,k}^{(0)}=\log\{\max(m_{i,k},10^{-6})\},\quad k\in\{C,N,E\},
\end{equation}

再在 logit 空间叠加风险压力与物理压力：

\begin{equation}
\tilde\ell_{i,E}(t)=\ell_{i,E}^{(0)}+0.16r_t+0.24(0.8h_t),\quad
\tilde\ell_{i,C}(t)=\ell_{i,C}^{(0)}-0.11r_t-0.18(0.8h_t),
\end{equation}

其中 \(E\) 表示升级列，\(C\) 表示缓和列；若当前已处于升级状态，则升级列额外增加 \(0.07r_t+0.11(0.8h_t)\) 的状态惯性项；若当前处于缓和状态，则维持列增加 \(0.04r_t\)。第 120 天和第 150 天后不再按日历时间机械提高缓和概率，而是设置
\[
\lambda_t=0.75\cdot \mathrm{clip}\left(\frac{g_t-0.45}{0.35},0,1\right)\cdot \mathrm{clip}(1-0.75\cdot0.8h_t,0,1),
\]
并仅在 \(\lambda_t>0\) 时提高缓和倾向、压低升级倾向。最终时变概率由
\[
m_{i,k}(t)=\frac{\exp\{\tilde\ell_{i,k}(t)\}}{\sum_j\exp\{\tilde\ell_{i,j}(t)\}}
\]
得到。这一 logit--softmax 设定使状态转移始终位于合法概率单纯形内，同时避免“概率直接加减再裁剪”的人为边界效应。\cref{tab:state-transition-snapshot} 给出从“维持”状态出发时的关键外推日概率快照。

\begin{table}[!htbp]
\centering
\caption{长期状态转移矩阵与时变概率快照}
\begin{subtable}[t]{0.45\linewidth}
\centering
\caption{基础矩阵}
\label{tab:state-transition-matrix}
\footnotesize
\begin{tabular}{lrrr}
\toprule
状态 & 缓和 & 维持 & 升级 \\
\midrule
缓和 & 0.478 & 0.522 & 0.000 \\
维持 & 0.065 & 0.880 & 0.055 \\
升级 & 0.000 & 0.255 & 0.745 \\
\bottomrule
\end{tabular}
\end{subtable}
\hfill
\begin{subtable}[t]{0.50\linewidth}
\centering
\caption{当前状态为“维持”}
\label{tab:state-transition-snapshot}
\footnotesize
\begin{tabular}{rrrr}
\toprule
外推日 & 缓和 & 维持 & 升级 \\
\midrule
60 & 0.060 & 0.879 & 0.061 \\
90 & 0.058 & 0.878 & 0.064 \\
120 & 0.063 & 0.878 & 0.059 \\
150 & 0.057 & 0.878 & 0.065 \\
180 & 0.058 & 0.878 & 0.064 \\
\bottomrule
\end{tabular}
\end{subtable}
\end{table}

\FloatBarrier

\cref{tab:state-transition-snapshot} 显示，在物理压力、OVX 和 GPR 风险读数共同约束下，早期仍保留一定升级概率；第 120 天后若悲观路径中 SPR 预算耗尽、剩余缺口仍存在，缓和概率不会因为时间推进而自动上升。因此，状态转移模型并不是把长期价格强行拉成随机噪声，而是在三情景中心路径之上加入“资源约束影响尾部、后期仍可能跳涨”的事件状态结构。

\begin{figure}[!htbp]
\centering
\includegraphics[width=0.88\linewidth]{长期状态转移情景树.png}
\caption{长期状态转移情景树}
\label{fig:state-transition-fan}
\end{figure}

\FloatBarrier

\begin{table}[!htbp]
\centering
\caption{物理压力与 GPR/OVX 风险约束消融结果}
\label{tab:state-risk-ablation}
\small
\begin{tabularx}{\linewidth}{Xrrrr}
\toprule
模型设定 & 突破 120 概率 & 突破 130 概率 & 最高价 P95 & 第 180 天 P95 \\
\midrule
历史基础矩阵 & 35.6\% & 2.2\% & 127.73 & 106.81 \\
历史基础矩阵 + 物理压力约束 & 41.6\% & 2.8\% & 128.48 & 108.56 \\
历史基础矩阵 + 物理压力 + GPR/OVX 风险约束 & 47.9\% & 3.8\% & 129.10 & 108.95 \\
\bottomrule
\end{tabularx}
\normalsize
\end{table}

\FloatBarrier

\cref{tab:state-risk-ablation} 进一步说明，物理压力会抬升第 180 天上尾分位，GPR/OVX 风险约束则进一步提高进入高价平台的条件概率。这符合二者的经济含义：SPR、库存和剩余缺口决定长期尾部是否有物理支撑，GPR 和 OVX 则更接近地缘风险与市场隐含波动率变量，适合约束状态切换和尾部风险，而不应被解释为短期方向价格预测器。

结果显示，中性情景下价格在 180 天附近回落至约 93.26 美元/桶；悲观情景下，若供应中断偏大、SPR 释放弱、绕道恢复慢且风险溢价维持高位，价格仍可能再次升至 120 美元/桶以上，并在外推期达到约 129.71 美元/桶的高点；由于 SPR 等效预算耗尽，第 180 天悲观价格抬升至约 111.71 美元/桶。\cref{fig:state-transition-fan} 的状态转移增强模型进一步给出第 180 天 5\%--95\% 条件区间 90.20--108.95 美元/桶，外推期最高价 P95 为 129.10 美元/桶，突破 120 美元/桶的条件概率为 47.9\%，说明长期结果应重点关注尾部区间和状态切换风险。

\begin{table}[!htbp]
\centering
\caption{中长期油价调节模型的可信度评价框架}
\begin{tabularx}{\linewidth}{lX X}
\toprule
评价维度 & 本文证据 & 解释边界 \\
\midrule
机制一致性 & SPR 自适应收缩、长期需求弹性、供给恢复和风险溢价衰减进入同一递推式 & 检验情景逻辑是否自洽 \\
外部数量级校验 & EIA 周度 SPR、商业库存和美国原油产量，叠加 JODI 多国月度产量、库存、进出口、需求和炼厂产出、OPEC 全球供需平衡表，以及 OVX 隐含波动率 & 用于数量级和风险约束 \\
单因素敏感性 & 第 180 天价格对封锁风险衰减速度、地缘风险权重和制度风险强度最敏感 & 识别关键变量，不给出联合概率 \\
蒙特卡洛情景树 & 第 180 天价格 P05--P95 为 88.85--107.07 美元/桶，最高价突破 120 概率约 10.3\% & 条件概率，不是无条件市场概率 \\
状态转移情景树 & 第 180 天价格 P05--P95 为 90.20--108.95 美元/桶，外推期最高价 P95 为 129.10 美元/桶，突破 120 概率约 47.9\% & 用于解释平滑中心线之外的事件状态切换和短期扰动 \\
\bottomrule
\end{tabularx}
\end{table}

\FloatBarrier

从价格平衡点看，乐观情景最终回落至约 87.05 美元/桶，说明若 SPR 可用上限较高、绕道运输恢复充分且长期需求弹性发挥作用，油价可以逐步脱离 110--120 美元平台。中性情景第 180 天约为 93.26 美元/桶，更接近政策缓冲与市场风险重新平衡后的中心水平；此时商业库存和 SPR 均作为缓冲参与，且物理缺口基本闭合，说明价格回落不是依赖单一政策变量机械满额托底。悲观情景第 180 天仍为 111.71 美元/桶，说明若供应中断规模接近上界、SPR 等效预算耗尽且制度风险溢价持续，市场仍可能维持相对高位，并保留二次跳涨尾部风险。

中性第 180 天物理缺口已基本闭合，但价格仍高于战前 75 美元/桶，原因是模型中仍保留不确定性和制度风险溢价。该设定表示：即便物理缺口基本闭合，只要霍尔木兹封锁的制度状态没有完全解除，市场仍会对运输保险、航运安全、库存再补充和再次升级风险给出残余溢价。因此 93.26 美元/桶应理解为“物理缺口闭合但制度风险尚未完全消失”条件下的中心路径。

商业库存剩余率和剩余供需缺口风险的完整图形放入附录 D。正文保留其核心结论：悲观路径的尾部风险主要来自 SPR 等效预算耗尽、商业库存消耗和剩余缺口同时存在，而不是由中性路径本身决定。
