# sql 目录说明

本目录保存 PostgreSQL 数据库方案草案。

当前项目主线没有依赖数据库；论文和模型复现均基于 CSV 文件完成。保留 SQL 文件的目的，是为后续扩展数据入库、查询审计和可视化服务做准备。

| 文件 | 作用 |
|---|---|
| `001_init_schema.sql` | 数据库表结构草案 |
| `002_load_raw_brent_placeholder.sql` | 原始布伦特数据入库占位脚本 |

除非后续明确切换到数据库工作流，否则不要把数据库作为论文复现的必要条件。
