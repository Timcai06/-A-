# 下一阶段目标：短期模型代码与建模质量提升

## 1. 核心目标

下一阶段先不以论文美化为主，而是从代码和建模层面继续提高短期模型质量。

当前短期模型已经可以作为主模型使用：机制递推主模型 RMSE 为 3.44，在线残差校正后 RMSE 为 3.38。但这轮提升幅度有限，说明继续优化不能只靠 46 个交易日内部调参，而要引入更系统的历史训练、滚动验证和模型对比。

下一阶段的目标是：

- 判断短期模型是否还有实质性精度提升空间。
- 用 2017-2026 历史数据训练和验证辅助模型。
- 比较机制模型、传统时间序列、机器学习模型和机制加残差修正模型。
- 如果机器学习辅助确实更好，就把它作为短期增强版保留；如果提升有限，就证明当前机制模型已经接近该数据条件下的合理上限。

## 2. 建模方向

### 2.1 机制模型继续作为主模型

综合机制递推模型仍然是短期主模型。

原因：

- 它直接对应赛题机制：供应中断、SPR、库存、绕道、需求收缩和恐慌衰减。
- 它有明确经济含义，方便解释为什么油价停留在 110-120 美元附近。
- 它已经打败朴素上一日基准、滚动 ARIMA 和其他简单基准。

后续新增模型不能替代它，只能作为对照、残差修正或稳健性证明。

### 2.2 引入机器学习残差修正

下一步优先实现：

```text
机制递推模型预测值 + 机器学习残差修正 = 短期增强预测值
```

机器学习模型不直接预测油价，而是预测：

```text
真实价格 - 机制模型预测价格
```

这样做的好处是：

- 保留机制模型的解释性。
- 让机器学习只处理主模型没捕捉到的局部偏差。
- 避免黑箱模型在小样本冲突窗口里直接过拟合油价路径。

候选模型：

- Ridge / Lasso：低自由度，适合小样本残差修正。
- Random Forest：捕捉非线性，但要控制树深度。
- Gradient Boosting：可能提升局部拟合，但需防止过拟合。
- SVR：适合小样本非线性回归。
- 小型 MLP：只作为补充对照，不作为主线。

暂不优先使用 LSTM/Transformer。原因是样本太少、解释性弱，很容易被评委质疑为背答案。

### 2.3 引入状态空间或 Kalman Filter

当前机制模型是确定性递推，所以曲线天然比较平滑。短期油价存在日度噪声、跳动和市场情绪扰动，可以考虑加入状态空间层：

```text
机制模型给出理论价格中心
Kalman Filter 根据历史波动动态修正价格状态
```

这类方法比纯机器学习更接近金融时间序列建模，也更容易解释：

- 状态噪声表示市场短期非理性波动。
- 观测噪声表示每日价格扰动。
- 机制模型仍然决定价格中心。

验收标准不是只看 RMSE，还要看是否改善中期平台和拐点表现。

## 3. 历史数据如何使用

2017-2026 的完整价格数据不能只作为背景图使用，下一阶段要把它用于训练和验证。

### 3.1 特征工程

从历史价格构造以下特征：

| 特征类型 | 示例 |
|---|---|
| 滞后收益率 | 1 日、3 日、5 日、10 日收益率 |
| 波动率 | 5 日、10 日、20 日滚动波动率 |
| 均线偏离 | 当前价格相对 5 日、20 日、60 日均线偏离 |
| 动量反转 | 连续上涨/下跌天数，短期涨跌幅分位数 |
| 价格位置 | 当前价格在历史滚动窗口中的分位数 |
| 机制特征 | 供应缺口、风险溢价、缓冲折价、恐慌因子 |
| 残差特征 | 前 1 日、前 3 日、前 5 日模型误差 |

### 3.2 训练方式

不能把 2026 冲突窗口直接拿来训练再汇报测试效果。

建议流程：

1. 用 2017-2025 历史数据构造普通市场训练样本。
2. 用历史滚动窗口检验机器学习模型是否稳定。
3. 用 2026 冲突窗口作为最终外部测试。
4. 对比机制主模型、机器学习模型和机制加残差修正模型。

### 3.3 预测目标

优先预测收益率或残差，而不是直接预测价格水平。

可选目标：

- 下一日收益率。
- 下一日价格变化。
- 机制模型残差。
- 未来 3 日平均残差。

直接预测价格水平容易变成“复制昨天价格”，不利于证明模型真的有预测能力。

## 4. 代码任务拆解

### 任务一：新增历史特征构造模块

建议文件：

```text
src/analysis/short_term_ml_features.py
```

输出：

```text
output/calibration/短期机器学习特征样本.csv
```

要求：

- 使用中文 CSV 文件名。
- 每个特征必须可追溯，不引入无来源数据。
- 明确哪些特征在预测日之前可得，避免未来函数。

### 任务二：新增机器学习基准与残差修正模块

建议文件：

```text
src/analysis/short_term_ml_residual_model.py
```

输出：

```text
output/calibration/短期机器学习模型对比.csv
output/calibration/短期机器学习残差修正路径.csv
output/reports/短期机器学习残差修正报告.md
paper/figures/短期机器学习残差修正对比.png
```

模型至少包含：

- 朴素上一日基准。
- 滚动 ARIMA 或已有时间序列基准。
- Ridge 残差修正。
- Random Forest 或 Gradient Boosting 残差修正。
- 当前机制递推主模型。
- 当前在线残差校正模型。

### 任务三：新增滚动验证

建议文件：

```text
src/analysis/short_term_rolling_validation.py
```

输出：

```text
output/calibration/短期历史滚动验证结果.csv
output/reports/短期历史滚动验证报告.md
paper/figures/短期历史滚动验证误差分布.png
```

核心问题：

- 机器学习模型是否只在 2026 冲突窗口碰巧有效。
- 机制模型在非霍尔木兹窗口是否失效，是否符合“事件模型”的定位。
- 残差修正是否稳定降低误差，还是只改善个别日期。

### 任务四：可选状态空间增强

建议文件：

```text
src/analysis/short_term_state_space_filter.py
```

输出：

```text
output/calibration/短期状态空间滤波结果.csv
output/reports/短期状态空间滤波报告.md
paper/figures/短期状态空间滤波效果.png
```

验收标准：

- RMSE 或 MAE 有实质改善。
- 中期平台误差下降。
- 不牺牲太多方向命中率。
- 方法解释清楚，不变成纯平滑曲线。

## 5. 质量验收标准

下一阶段不只看 RMSE。

必须同时检查：

| 维度 | 验收标准 |
|---|---|
| 整体误差 | RMSE、MAE、MAPE 至少不劣于当前主模型 |
| 基准优势 | 必须继续优于朴素上一日基准和滚动 ARIMA |
| 方向能力 | 方向命中率不能明显劣化 |
| 拐点表现 | 前期暴涨和后期反转窗口不能慢半拍 |
| 中期平台 | 当前最难解释的中期平台 RMSE 要重点下降 |
| 过拟合风险 | 不能只在 46 天窗口内调参变好 |
| 可解释性 | 新增模型必须说明它修正了什么偏差 |
| 可复现性 | 固定随机种子，输出完整 CSV 和报告 |

如果一个新模型只降低 RMSE，但方向命中率明显下降、拐点表现变差或解释性很弱，就不能作为最终短期主模型。

## 6. 阶段结论预期

下一阶段结束时，需要得到一个明确判断：

### 情况 A：机制加机器学习残差修明确实更好

如果残差修正能稳定提升 RMSE、MAE、中期平台误差，并且不明显损害方向命中率，则将它作为短期增强版保留。

论文表述应为：

```text
综合机制递推模型给出主要价格形成机制，机器学习残差层用于修正可学习的短期误差惯性。
```

### 情况 B：机器学习提升有限

如果机器学习只带来很小提升，或者提升来自牺牲方向命中率、过拟合局部日期，则不把它作为主结果。

论文表述应为：

```text
本文尝试了数据驱动残差修正，但在极短冲突窗口下，黑箱模型提升有限；综合机制模型在解释性、稳健性和误差之间取得更好平衡。
```

这同样是有价值的结果，因为它能证明我们不是没考虑计算机方法，而是经过比较后选择了更适合赛题的模型形态。

## 7. 下一步执行顺序

建议执行顺序：

1. 先写历史特征构造模块。
2. 再做 Ridge / Random Forest 残差修正对比。
3. 然后做滚动验证和冲突窗口最终测试。
4. 如果残差修正有效，再更新短期模型报告和论文。
5. 如果残差修正提升有限，再转向状态空间/Kalman Filter。

本阶段的底线是：宁可承认机器学习提升有限，也不能为了让曲线更漂亮而制造不可防御的过拟合模型。

## 8. 当前执行进展

已完成第一批代码落地：

| 任务 | 状态 | 产物 |
|---|---|---|
| 历史特征构造 | 已完成 | `src/analysis/short_term_ml_features.py` |
| Ridge 机器学习基准 | 已完成 | `src/analysis/short_term_ml_residual_model.py` |
| 历史滚动验证 | 已完成 | `src/analysis/short_term_rolling_validation.py` |

当前关键结果：

- 历史特征样本覆盖 2017-09-01 至 2026-05-05，共 2237 行，其中完整可训练样本 2197 行。
- 单独的历史 Ridge 收益率模型在冲突窗口 RMSE 为 4.557，不如机制递推主模型，说明普通历史价格模型不能单独解释霍尔木兹冲突窗口。
- 机制递推主模型 RMSE 为 3.440。
- 在线残差校正模型 RMSE 为 3.383。
- 机制 + Ridge 收益率修正 RMSE 为 3.355，方向命中率为 68.9%，暂时是当前短期增强结果里误差和方向表现都较好的方案。
- 历史 46 日滚动验证显示，Ridge 相对朴素基准的 RMSE 改善率中位数为 -0.57%，优于朴素基准的窗口占比约 44.0%，说明纯历史机器学习模型并不稳定强于随机游走。

当前判断：

```text
机器学习不能替代机制主模型，但可以作为有限的收益率修正层。
```

下一步应重点验证“机制 + Ridge 收益率修正”是否在更多分段指标、拐点指标和过拟合检验中保持优势。如果它只是整体 RMSE 小幅变好，但中期平台或拐点表现变差，就不能把它升级为最终短期增强主线。
